<main class="hero">
  <div class="hero-glow hero-glow-one" aria-hidden="true"></div>
  <div class="hero-glow hero-glow-two" aria-hidden="true"></div>

  <div class="hero-inner">
    <div class="hero-copy">
      <p class="eyebrow">Perfumería árabe y de autor</p>
      <h1>Tu próxima fragancia empieza con una sensación.</h1>
      <p class="hero-text">
        Descubre aromas con carácter, originales y disponibles en Chile. Te
        ayudamos a encontrar el perfume que mejor habla de ti.
      </p>

      <form class="hero-search" data-catalog-search>
        <label class="sr-only" for="hero-search-input">Buscar perfumes</label>
        <input
          id="hero-search-input"
          type="search"
          placeholder="Busca por perfume, marca o aroma"
          autocomplete="off"
        />
        <button type="submit">Buscar</button>
      </form>

      <div class="hero-actions">
        <a class="primary-action" href="/perfumes">Explorar catálogo</a>
        <a
          class="secondary-action"
          href="https://wa.me/56935226409?text=Hola%20Nuite%2C%20necesito%20ayuda%20para%20elegir%20un%20perfume"
          target="_blank"
          rel="noreferrer"
        >
          Recibir asesoría
        </a>
      </div>

      <ul class="hero-proof" aria-label="Ventajas de comprar en Nuite">
        <li><strong>100%</strong> perfumes originales</li>
        <li><strong>Stock</strong> actualizado</li>
        <li><strong>Envíos</strong> a todo Chile</li>
      </ul>
    </div>

    <div class="hero-showcase" aria-label="Selección destacada de perfumes Nuite">
      <div class="showcase-copy">
        <span>Selección Nuite</span>
        <strong>Aromas que dejan huella</strong>
      </div>

      <div class="product-stage">
        <div class="product-card product-card-back">
          <img
            src="/img/yara-tous.webp"
            alt="Yara Tous de Lattafa"
            width="1080"
            height="1080"
            decoding="async"
          />
        </div>
        <div class="product-card product-card-front">
          <span class="spotlight-badge">Favorito</span>
          <img
            src="/img/asad.webp"
            alt="Asad de Lattafa"
            width="1080"
            height="1080"
            fetchpriority="high"
          />
        </div>
        <div class="product-card product-card-side">
          <img
            src="/img/mega.webp"
            alt="Odyssey Mega de Armaf"
            width="1200"
            height="630"
            decoding="async"
          />
        </div>
      </div>

      <a href="/perfumes?marca=Lattafa#catalogo" class="showcase-link">Descubrir Lattafa <span>→</span></a>
    </div>
  </div>
</main>

<script>
  document.querySelectorAll("[data-catalog-search]").forEach((form) => {
    form.addEventListener("submit", (event) => {
      event.preventDefault();
      const input = form.querySelector("input");
      const value = input?.value?.trim() || "";
      window.location.href = `/perfumes?buscar=${encodeURIComponent(value)}#catalogo`;
    });
  });
</script>

<style>
  .hero {
    position: relative;
    overflow: hidden;
    background:
      radial-gradient(circle at 78% 30%, rgba(255, 196, 0, 0.13), transparent 27%),
      linear-gradient(135deg, #050505 0%, #101010 55%, #050505 100%);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .hero-inner {
    position: relative;
    z-index: 2;
    max-width: 1380px;
    min-height: 670px;
    margin: 0 auto;
    padding: 76px 28px 82px;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(460px, 0.84fr);
    align-items: center;
    gap: 72px;
  }

  .hero-copy {
    max-width: 720px;
  }

  .eyebrow {
    margin: 0 0 22px;
    color: var(--gold);
    font-size: 12px;
    font-weight: 900;
    letter-spacing: 0.3em;
    text-transform: uppercase;
  }

  h1 {
    max-width: 760px;
    margin: 0;
    font-size: clamp(48px, 5.2vw, 78px);
    line-height: 0.98;
    letter-spacing: -0.055em;
    text-wrap: balance;
  }

  .hero-text {
    max-width: 640px;
    margin: 28px 0 0;
    color: #c7c7c7;
    font-size: clamp(17px, 1.4vw, 20px);
    line-height: 1.7;
  }

  .hero-search {
    max-width: 670px;
    margin-top: 34px;
    padding: 7px;
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 8px;
    background: #fff;
    border-radius: 18px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.32);
  }

  .hero-search input {
    width: 100%;
    min-width: 0;
    height: 54px;
    padding: 0 18px;
    border: 0;
    outline: 0;
    background: transparent;
    color: #111;
    font: inherit;
    font-size: 16px;
  }

  .hero-search button,
  .primary-action {
    border: 0;
    border-radius: 13px;
    background: var(--gold);
    color: #050505;
    font: inherit;
    font-size: 14px;
    font-weight: 900;
    cursor: pointer;
  }

  .hero-search button {
    min-width: 116px;
    padding: 0 24px;
  }

  .hero-actions {
    margin-top: 22px;
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .hero-actions a {
    min-height: 48px;
    padding: 0 22px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 13px;
    font-size: 14px;
    font-weight: 900;
    transition: transform 0.2s ease, background 0.2s ease;
  }

  .hero-actions a:hover {
    transform: translateY(-2px);
  }

  .secondary-action {
    border: 1px solid rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.05);
    color: #fff;
  }

  .hero-proof {
    margin: 34px 0 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 18px 30px;
    color: #9c9c9c;
    font-size: 13px;
    list-style: none;
  }

  .hero-proof li {
    position: relative;
    padding-left: 16px;
  }

  .hero-proof li::before {
    content: "";
    position: absolute;
    top: 0.45em;
    left: 0;
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--gold);
  }

  .hero-proof strong {
    color: #fff;
  }

  .hero-showcase {
    position: relative;
    min-height: 520px;
    padding: 34px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.11);
    border-radius: 38px;
    background:
      linear-gradient(145deg, rgba(255, 255, 255, 0.08), transparent 48%),
      #151515;
    box-shadow: 0 30px 100px rgba(0, 0, 0, 0.4);
  }

  .hero-showcase::after {
    content: "";
    position: absolute;
    width: 330px;
    height: 330px;
    right: -90px;
    bottom: -130px;
    border-radius: 50%;
    background: var(--gold);
    filter: blur(10px);
    opacity: 0.16;
  }

  .showcase-copy {
    position: relative;
    z-index: 4;
    display: flex;
    flex-direction: column;
    gap: 7px;
  }

  .showcase-copy span {
    color: var(--gold);
    font-size: 11px;
    font-weight: 900;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .showcase-copy strong {
    max-width: 300px;
    font-size: 25px;
    line-height: 1.1;
  }

  .product-stage {
    position: relative;
    z-index: 3;
    height: 345px;
    margin-top: 12px;
  }

  .product-card {
    position: absolute;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 24px;
    background: #fff;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.45);
  }

  .product-card img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .product-card-front {
    z-index: 3;
    top: 16px;
    left: 50%;
    width: 250px;
    height: 300px;
    transform: translateX(-50%);
  }

  .product-card-back,
  .product-card-side {
    top: 76px;
    width: 180px;
    height: 224px;
    opacity: 0.82;
  }

  .product-card-back {
    left: 0;
    transform: rotate(-8deg);
  }

  .product-card-side {
    right: 0;
    transform: rotate(8deg);
  }

  .spotlight-badge {
    position: absolute;
    z-index: 2;
    top: 14px;
    left: 14px;
    padding: 7px 11px;
    border-radius: 999px;
    background: #0a0a0a;
    color: var(--gold);
    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .showcase-link {
    position: relative;
    z-index: 4;
    display: inline-flex;
    align-items: center;
    gap: 10px;
    color: #fff;
    font-size: 14px;
    font-weight: 900;
  }

  .showcase-link span {
    color: var(--gold);
    font-size: 20px;
  }

  .hero-glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    pointer-events: none;
  }

  .hero-glow-one {
    width: 300px;
    height: 300px;
    top: -130px;
    left: 18%;
    background: rgba(255, 196, 0, 0.1);
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  @media (max-width: 1050px) {
    .hero-inner {
      grid-template-columns: 1fr;
      gap: 52px;
      padding-top: 60px;
    }

    .hero-copy {
      max-width: 820px;
    }

    .hero-showcase {
      width: 100%;
      max-width: 720px;
      min-height: 500px;
      margin: 0 auto;
    }
  }

  @media (max-width: 620px) {
    .hero-inner {
      min-height: auto;
      padding: 48px 16px 56px;
      gap: 38px;
    }

    h1 {
      font-size: clamp(43px, 13vw, 60px);
    }

    .hero-text {
      margin-top: 22px;
      font-size: 16px;
    }

    .hero-search {
      grid-template-columns: 1fr;
      margin-top: 26px;
      border-radius: 17px;
    }

    .hero-search button {
      min-height: 48px;
    }

    .hero-actions {
      align-items: stretch;
      flex-direction: column;
    }

    .hero-actions a {
      width: 100%;
    }

    .hero-proof {
      display: grid;
      gap: 12px;
    }

    .hero-showcase {
      min-height: 440px;
      padding: 26px 20px;
      border-radius: 28px;
    }

    .product-stage {
      height: 300px;
    }

    .product-card-front {
      width: 205px;
      height: 255px;
    }

    .product-card-back,
    .product-card-side {
      top: 70px;
      width: 135px;
      height: 185px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-actions a {
      transition: none;
    }
  }
</style>
