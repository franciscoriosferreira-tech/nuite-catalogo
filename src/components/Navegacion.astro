---
const marcas = [
  "French Avenue",
  "Armaf",
  "Lattafa",
  "Maison Alhambra",
  "Paris Corner",
  "Rave",
];
---

<header class="site-header">
  <div class="header-main">
    <a href="/" class="brand" aria-label="Ir al inicio de Nuite Perfumes">
      <span class="logo-circle">
        <img src="/img/logo.png" alt="" width="64" height="64" />
      </span>
      <span class="brand-text">
        <strong>Nuit&eacute;</strong>
        <small>PERFUMES</small>
      </span>
    </a>

    <form class="header-search" data-catalog-search>
      <label class="sr-only" for="header-search-input">Buscar en el catálogo</label>
      <span aria-hidden="true" class="search-mark"></span>
      <input
        id="header-search-input"
        type="search"
        placeholder="¿Qué perfume estás buscando?"
        autocomplete="off"
      />
      <button type="submit">Buscar</button>
    </form>

    <div class="header-actions">
      <a
        href="https://instagram.com/nuiteperfumes"
        target="_blank"
        rel="noreferrer"
        class="quiet-link"
      >
        Instagram
      </a>
      <a
        href="https://wa.me/56935226409?text=Hola%20Nuite%2C%20necesito%20asesor%C3%ADa%20con%20un%20perfume"
        target="_blank"
        rel="noreferrer"
        class="contact-link"
      >
        Hablar por WhatsApp
      </a>
    </div>
  </div>

  <div class="nav-row">
    <nav class="menu" aria-label="Navegación principal">
      <a href="/" aria-current="page">Inicio</a>
      <a href="/#catalogo">Perfumes</a>

      <div class="dropdown">
        <button type="button" aria-haspopup="true">Marcas <span>⌄</span></button>
        <div class="dropdown-menu">
          {marcas.map((marca) => (
            <a href={`/?marca=${encodeURIComponent(marca)}#catalogo`}>{marca}</a>
          ))}
        </div>
      </div>

      <a href="/?genero=Hombre#catalogo">Hombre</a>
      <a href="/?genero=Mujer#catalogo">Mujer</a>
      <a href="/?genero=Unisex#catalogo">Unisex</a>
      <a href="/ofertas">Ofertas</a>
      <a href="/contacto">Contacto</a>
    </nav>
  </div>

  <div class="benefit-strip" aria-label="Información de compra">
    <span><i></i> Envíos a todo Chile</span>
    <span><i></i> Asesoría personalizada</span>
    <span><i></i> Compra directa por WhatsApp</span>
    <span><i></i> Stock actualizado</span>
  </div>
</header>

<script>
  document.querySelectorAll("[data-catalog-search]").forEach((form) => {
    form.addEventListener("submit", (event) => {
      event.preventDefault();
      const input = form.querySelector("input");
      const value = input?.value?.trim() || "";

      if (window.location.pathname !== "/") {
        window.location.href = `/?buscar=${encodeURIComponent(value)}#catalogo`;
        return;
      }

      window.dispatchEvent(new CustomEvent("nuite:buscar", { detail: value }));
      document.querySelector("#catalogo")?.scrollIntoView({ behavior: "smooth" });
    });
  });
</script>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 80;
    color: #111;
    background: #fff;
    box-shadow: 0 10px 35px rgba(0, 0, 0, 0.08);
  }

  .header-main {
    max-width: 1380px;
    min-height: 84px;
    margin: 0 auto;
    padding: 12px 28px;
    display: grid;
    grid-template-columns: auto minmax(340px, 1fr) auto;
    align-items: center;
    gap: 34px;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    color: #080808;
  }

  .logo-circle {
    width: 56px;
    height: 56px;
    display: grid;
    place-items: center;
    overflow: hidden;
    border-radius: 50%;
    background: #050505;
  }

  .logo-circle img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .brand-text {
    display: flex;
    flex-direction: column;
    line-height: 1;
  }

  .brand-text strong {
    font-family: Georgia, "Times New Roman", serif;
    font-size: 31px;
    letter-spacing: -0.04em;
  }

  .brand-text small {
    margin-top: 5px;
    font-size: 9px;
    font-weight: 900;
    letter-spacing: 0.32em;
  }

  .header-search {
    height: 50px;
    display: grid;
    grid-template-columns: 22px 1fr auto;
    align-items: center;
    gap: 10px;
    padding: 5px 6px 5px 18px;
    border: 1px solid #e4e4e4;
    border-radius: 999px;
    background: #f3f3f3;
  }

  .search-mark {
    position: relative;
    width: 14px;
    height: 14px;
    border: 2px solid #222;
    border-radius: 50%;
  }

  .search-mark::after {
    content: "";
    position: absolute;
    right: -5px;
    bottom: -3px;
    width: 7px;
    height: 2px;
    background: #222;
    transform: rotate(45deg);
    transform-origin: left center;
  }

  .header-search input {
    width: 100%;
    min-width: 0;
    height: 38px;
    padding: 0;
    border: 0;
    outline: 0;
    background: transparent;
    color: #111;
    font: inherit;
    font-size: 14px;
  }

  .header-search button {
    height: 38px;
    padding: 0 20px;
    border: 0;
    border-radius: 999px;
    background: #111;
    color: #fff;
    font-size: 12px;
    font-weight: 900;
    cursor: pointer;
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .header-actions a {
    min-height: 42px;
    padding: 0 17px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 12px;
    font-size: 12px;
    font-weight: 900;
    white-space: nowrap;
  }

  .quiet-link {
    border: 1px solid #dedede;
    color: #111;
  }

  .contact-link {
    background: var(--gold);
    color: #080808;
  }

  .nav-row {
    border-top: 1px solid #ececec;
    border-bottom: 1px solid #e5e5e5;
  }

  .menu {
    max-width: 1380px;
    min-height: 47px;
    margin: 0 auto;
    padding: 0 28px;
    display: flex;
    align-items: center;
    gap: 28px;
    color: #111;
    font-size: 13px;
    font-weight: 850;
  }

  .menu > a,
  .dropdown > button {
    height: 47px;
    display: inline-flex;
    align-items: center;
    border: 0;
    border-bottom: 2px solid transparent;
    background: transparent;
    color: inherit;
    font: inherit;
    cursor: pointer;
    white-space: nowrap;
  }

  .menu > a:hover,
  .menu > a[aria-current="page"],
  .dropdown > button:hover {
    border-bottom-color: var(--gold);
  }

  .dropdown {
    position: relative;
  }

  .dropdown button span {
    margin-left: 7px;
  }

  .dropdown-menu {
    position: absolute;
    top: calc(100% - 1px);
    left: -18px;
    width: 240px;
    padding: 12px;
    display: none;
    border: 1px solid #292929;
    border-radius: 0 0 18px 18px;
    background: #111;
    color: #fff;
    box-shadow: 0 24px 50px rgba(0, 0, 0, 0.25);
  }

  .dropdown:hover .dropdown-menu,
  .dropdown:focus-within .dropdown-menu {
    display: grid;
  }

  .dropdown-menu a {
    padding: 11px 13px;
    border-radius: 10px;
    font-size: 13px;
  }

  .dropdown-menu a:hover {
    background: var(--gold);
    color: #080808;
  }

  .benefit-strip {
    min-height: 38px;
    padding: 8px 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 64px;
    overflow-x: auto;
    background: #f6f6f6;
    color: #313131;
    font-size: 11px;
    font-weight: 800;
    white-space: nowrap;
    scrollbar-width: none;
  }

  .benefit-strip::-webkit-scrollbar {
    display: none;
  }

  .benefit-strip span {
    display: inline-flex;
    align-items: center;
    gap: 9px;
  }

  .benefit-strip i {
    width: 7px;
    height: 7px;
    border-radius: 2px;
    background: var(--gold);
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  @media (max-width: 1120px) {
    .header-main {
      grid-template-columns: auto 1fr;
      gap: 18px;
    }

    .header-search {
      grid-column: 1 / -1;
      grid-row: 2;
    }

    .header-actions {
      justify-self: end;
    }

    .nav-row {
      overflow-x: auto;
      scrollbar-width: none;
    }

    .nav-row::-webkit-scrollbar {
      display: none;
    }

    .menu {
      width: max-content;
      min-width: 100%;
    }

    .benefit-strip {
      justify-content: flex-start;
      gap: 38px;
    }
  }

  @media (max-width: 620px) {
    .site-header {
      position: relative;
    }

    .header-main {
      min-height: auto;
      padding: 10px 16px 12px;
      gap: 12px;
    }

    .logo-circle {
      width: 44px;
      height: 44px;
    }

    .brand-text strong {
      font-size: 24px;
    }

    .quiet-link {
      display: none !important;
    }

    .header-actions a {
      min-height: 38px;
      padding: 0 13px;
      font-size: 10px;
    }

    .header-search {
      height: 48px;
      padding-left: 16px;
    }

    .header-search button {
      padding: 0 15px;
    }

    .menu {
      gap: 22px;
      padding: 0 16px;
      font-size: 12px;
    }

    .dropdown-menu {
      display: none !important;
    }

    .benefit-strip {
      padding-inline: 16px;
      gap: 30px;
    }
  }
</style>
